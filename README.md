ORM and JPA
ORM (Object-Relational Mapping):objects se table create krne ko orm bolte hae like int rollno toh orm use krte haeKey ORM Tools:
➢ Hibernate
➢ TopLink
➢ EclipseLink

JPA (Java Persistence API):
● JPA is a specification (not a framework) that defines how Java objects should
be persisted in relational databases.
● It standardizes the mapping of objects to relational databases and provides a
common interface for ORM tools like Hibernate.
● Hibernate, as one of the popular ORM frameworks, implements the JPA
specification.

Spring Data JPA further simplifies working with Hibernate and Spring ORM
by abstracting and automating database operations.